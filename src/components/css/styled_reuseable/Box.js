import styled from 'styled-components';
import { colors } from "../theme";
export default styled.div`
  background-color: ${colors.backgroundPrimary};
  border: 0.1rem ${colors.gamma} solid;
  font-size: 1.4rem;
  padding: 3rem 2rem 4rem 2rem;
`;
